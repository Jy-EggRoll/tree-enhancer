{
    "name": "eggroll-tree-enhancer",
    "displayName": "Tree Enhancer",
    "description": "在 VS Code 资源管理器中悬浮显示文件/文件夹的大小和修改时间",
    "version": "0.0.1",
    "engines": {
        "vscode": "^1.101.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./out/extension.js",
    "contributes": {
        "configuration": {
            "title": "Tree Enhancer",
            "properties": {
                "eggroll-tree-enhancer.maxCalculationTime": {
                    "type": "number",
                    "default": 5000,
                    "minimum": 1000,
                    "maximum": 60000,
                    "description": "文件夹大小计算的最大等待时间（毫秒）。超过此时间将停止计算并显示提示信息。"
                },
                "eggroll-tree-enhancer.fileSizeBase": {
                    "type": "number",
                    "enum": [
                        1000,
                        1024
                    ],
                    "default": 1000,
                    "description": "文件大小计算基底。选择 1000（十进制，使用 KB/MB/GB 等单位）或 1024（二进制，使用 KiB/MiB/GiB 等单位）。\n\n• 1000：符合国际标准（SI），硬盘厂商常用，显示的数值较大\n• 1024：传统计算机标准，操作系统常用，显示的数值较小但更精确反映二进制存储\n\n例如：1024000 字节\n• 使用 1000：显示为 1.02 MB\n• 使用 1024：显示为 1000 KiB"
                }
            }
        }
    },
    "scripts": {
        "vscode:prepublish": "npm run compile",
        "compile": "tsc -p ./",
        "watch": "tsc -watch -p ./",
        "pretest": "npm run compile && npm run lint",
        "lint": "eslint src",
        "test": "vscode-test"
    },
    "devDependencies": {
        "@types/vscode": "^1.101.0",
        "@types/mocha": "^10.0.10",
        "@types/node": "20.x",
        "@typescript-eslint/eslint-plugin": "^8.31.1",
        "@typescript-eslint/parser": "^8.31.1",
        "eslint": "^9.25.1",
        "typescript": "^5.8.3",
        "@vscode/test-cli": "^0.0.11",
        "@vscode/test-electron": "^2.5.2"
    }
}