{
    "configurations": [
        {
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--profile=扩展测试" // 配置一个没有任何扩展的配置文件，方便扩展开发
            ],
            "name": "Run Extension",
            "outFiles": [
                "${workspaceFolder}/dist/**/*.js"
            ],
            "preLaunchTask": "npm: compile",
            "request": "launch",
            "type": "extensionHost"
        }
    ]
}