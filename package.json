{
  "name": "eggroll-tree-enhancer",
  "displayName": "Tree Enhancer",
  "description": "在 VS Code 资源管理器中悬浮显示文件/文件夹的详细信息（大小、修改时间、文件数量等）",
  "version": "1.0.1",
  "publisher": "Jy-EggRoll",
  "icon": "icon.png",
  "author": {
    "name": "EggRoll"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Jy-EggRoll/tree-enhancer.git"
  },
  "bugs": {
    "url": "https://github.com/Jy-EggRoll/tree-enhancer/issues"
  },
  "homepage": "https://github.com/Jy-EggRoll/tree-enhancer#readme",
  "keywords": [
    "file",
    "folder",
    "explorer",
    "size",
    "tooltip",
    "tree",
    "enhancer",
    "chinese",
    "资源管理器"
  ],
  "engines": {
    "vscode": "^1.101.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Tree Enhancer",
      "properties": {
        "eggroll-tree-enhancer.maxCalculationTime": {
          "type": "number",
          "default": 5000,
          "minimum": 1000,
          "maximum": 60000,
          "description": "文件夹大小计算的最大等待时间（毫秒）。超过此时间将停止计算并显示提示信息。最大可以设置为 60 秒。"
        },
        "eggroll-tree-enhancer.fileSizeBase": {
          "type": "number",
          "enum": [
            1000,
            1024
          ],
          "default": 1000,
          "description": "文件大小计算基底。选择 1000（十进制，使用 KB / MB / GB 等单位）或 1024（二进制，使用 KiB / MiB / GiB 等单位）。\n\n- 1000：符合国际标准（SI）\n- 1024：传统计算机标准，操作系统常用\n\n例如：1024000 字节\n- 使用 1000：显示为 1.02 MB\n- 使用 1024：显示为 1000 KiB"
        },
        "eggroll-tree-enhancer.debugMode": {
          "type": "boolean",
          "default": false,
          "description": "启用调试模式。开启后将在控制台输出详细的扩展运行日志，便于诊断问题。\n\n- false（默认）：安静模式，只输出严重错误\n- true：调试模式，输出详细的计算过程和状态信息\n\n注意：调试模式会增加控制台输出，建议仅在遇到问题时临时开启。"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "20.x",
    "@types/vscode": "^1.101.0",
    "@typescript-eslint/eslint-plugin": "^8.31.1",
    "@typescript-eslint/parser": "^8.31.1",
    "@vscode/test-cli": "^0.0.11",
    "@vscode/test-electron": "^2.5.2",
    "eslint": "^9.25.1",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "vsce": "^2.15.0"
  }
}
